global:
  scrape_interval: 5s

scrape_configs:
  - job_name: socket-webrtc
    static_configs:
      - targets:
          - host.docker.internal:3000
        labels:
          service: socket-webrtc-backend
